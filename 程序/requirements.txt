# PDF paper translator (English -> Chinese) dependencies
# Install on Windows:  python -m pip install -r requirements.txt

pdfplumber>=0.11.0
reportlab>=4.0.0
pypdf>=4.0.0
requests>=2.28.0

# Preferred rendering backend (precise redaction + embedded CJK fonts).
# The tool still works without it (falls back to reportlab overlay).
pymupdf>=1.24.0
# Used by pymupdf to subset embedded fonts (smaller output files).
fonttools>=4.40.0

# OCR for scanned PDFs (models bundled in the wheel, works offline).
# Optional: without it, scanned PDFs are reported with an install hint.
rapidocr-onnxruntime>=1.4.0

# Word (.docx) & PowerPoint (.pptx) translation. MIT licensed - commercial-safe.
python-docx>=1.1.0
python-pptx>=1.0.0

# Native app window for the web UI (BSD). Optional: without it the UI opens
# in the default browser instead. Uses WebView2 on Windows, WebKit on macOS.
pywebview>=5.0
# macOS native-window backend for pywebview (Cocoa/WebKit via PyObjC).
# pip 按平台标记自动跳过非 mac 环境，不影响 Windows。
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"

# Test suite (development only): python -m pytest tests -q
# pytest>=8.0

# The GUI uses Python's built-in tkinter (no extra install needed).
